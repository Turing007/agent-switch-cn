{
  "name": "agent-switch-cn",
  "version": "0.1.1",
  "description": "国产 CLI Agent 模型配置切换器（Tauri v2 / Rust 桌面应用）",
  "private": true,
  "author": "agent-switch-cn",
  "license": "MIT",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "icon": "node scripts/gen-icon.js",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run build && tauri build",
    "typecheck": "vue-tsc --noEmit -p tsconfig.web.json --composite false"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "@vitejs/plugin-vue": "^5.1.4",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vue": "^3.4.38",
    "vue-tsc": "^2.1.6"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  }
}
